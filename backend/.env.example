# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key

# Gemini
GEMINI_API_KEY=your-gemini-api-key

# Evaluation — optional. Independent Gemini samples per prompt.
# Defaults to 8 when unset; a request can override it per evaluation.
SAMPLING_N=8
